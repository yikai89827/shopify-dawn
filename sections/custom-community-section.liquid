{%- liquid
  assign heading = section.settings.heading
  assign slogan = section.settings.slogan
  assign cta_text = section.settings.cta_text
  assign cta_link = section.settings.cta_link
  assign background = section.settings.background
-%}

<div class="community-section">
  <h2 class="section-heading">{{ heading }}</h2>

  {% if background != blank %}
    <div class="community-bg-container">
      {{ background | image_url: width: 2000 | image_tag: class: 'community-bg' }}
      <div class="slogan-container">
        <p class="slogan">{{ slogan }}</p>
      </div>
      <div class="button-container">
        <a href="{{ cta_link }}" class="button">{{ cta_text }}</a>
      </div>
    </div>
  {% endif %}
</div>

<style>
  .community-section {
    position: relative;
    text-align: center;
  }

  .community-bg-container {
    position: relative;
    margin: 20px 0;
  }

  .community-bg {
    width: 100%;
    height: auto;
    display: block;
  }

  .slogan-container {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 100%;
    padding: 0 20px;
  }

  .slogan {
    color: white;
    font-size: 2rem;
    text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.5);
  }

  .button-container {
    margin-top: 20px;
    padding-bottom: 30px;
  }

  @media screen and (max-width: 768px) {
    .slogan {
      font-size: 1.5rem;
    }
  }
</style>

{% schema %}
{
  "name": "Community Preview",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "区块标题",
      "default": "Community Preview"
    },
    {
      "type": "text",
      "id": "slogan",
      "label": "标语",
      "default": "Built on what we share."
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "按钮文字",
      "default": "Gather Community"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "按钮链接"
    },
    {
      "type": "image_picker",
      "id": "background",
      "label": "背景图片"
    }
  ],
  "presets": [
    {
      "name": "Community Preview"
    }
  ]
}
{% endschema %}
